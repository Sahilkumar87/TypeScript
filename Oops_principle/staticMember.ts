console.log(Math.PI);
console.log(Math.random());


class  Shery{
    static version = 1.0;

    static getRandom(){
        return Math.random();
    }
}

Shery.getRandom();
console.log(Shery.version);